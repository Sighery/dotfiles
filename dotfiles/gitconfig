[user]
{%@@ if profile in ["basic-manjaro", "sater-manjaro"] @@%}
	email = Sighery@users.noreply.github.com
	name = Sighery
{%@@ elif profile == "zoo-work" @@%}
	email = {{@@ zoo_work_email @@}}
	name = {{@@ short_full_name @@}}
{%@@ endif @@%}
[core]
	pager = less -x1,5
[status]
	showUntrackedFiles = all
